<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema Principal do Aplicativo -->
    <style name="Theme.LayoutsAndBackground" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Cor Primária -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Cor Secundária -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Fundo da Tela -->
        <item name="android:windowBackground">@color/backgroundColor</item>

        <!-- Estilo do Texto -->
        <item name="android:textColor">@color/textColor</item>
        <item name="android:fontFamily">@font/roboto_regular</item>

        <!-- Personalização de Botões -->
        <item name="buttonStyle">@style/CustomButtonStyle</item>
    </style>

    <!-- Estilo de Botões Personalizado -->
    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/buttonBackground</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">12dp</item>
    </style>

</resources>
